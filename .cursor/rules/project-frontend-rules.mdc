---
description: Lucio 的前端项目规则
alwaysApply: true
---

# 项目级前端规则

## 目录与文件规范
- 页面目录统一使用：`src/pages/页面名/index.tsx`
- 页面样式文件与页面同层放置，命名统一为：`index.module.less`
- 默认使用 Less Modules，不要随意改成 styled-components、css-in-js 或其他风格。Source: MEMORY.md#L28-L33

## 编码优先级
- 优先保持现有项目结构和风格一致，不要无故大改。
- 新增页面时，顺手补全路由、导航、必要的展示文案。
- 优先给出可直接运行的实现，而不是只给思路。
- 如果改动较大，要明确列出改动点、风险点、验证步骤。

## 调试与排错
- 先定位根因，再给修复方案。
- 不要只给模糊猜测。
- 优先选择改动小、稳定性高、维护成本低的方案。

## 面试友好输出
- 如果实现涉及 React、状态管理、工程化、浏览器、性能优化等高频考点，顺手补一句这个点在面试里可以怎么讲。
- 如果用户要“学习页 / 展示页 / 总结页”，默认做成“可运行 + 可复盘 + 可面试表达”的结构。
